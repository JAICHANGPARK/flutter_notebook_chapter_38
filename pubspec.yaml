name: flutter_notebook_chapter_38
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:

  go_router: ^14.2.0
  google_fonts: ^6.2.1
  gap: ^3.0.1
  smooth_page_indicator: ^1.1.0
  flutter_polygon: ^0.3.0+2
  flutter_svg:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
